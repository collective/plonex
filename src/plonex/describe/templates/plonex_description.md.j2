{%- set context = options["context"] -%}
{%- set log_level = context.options.get("log_level", "") -%}
{%- set plonex_version = context.options.get("plonex_version", "") -%}
# Project description

This file is auto-generated by plonex.
It describes the current project configuration.

The markdown version of this file is located at `{{ context.var_folder / "plonex_description" / "index.md" }}` and was generated at `{{ context.now }}`.

## Base facts


> ---
> **📂 Project Path**
> `{{ context.target }}`
>
> **🧩 PloneX Version**
> `{{ plonex_version }}`
>
> **🐍 Virtualenv Path**
> `{{ context.virtualenv_dir }}`
>
> **🔊 Log Level**
> `{{ log_level }}`
>
> ---
