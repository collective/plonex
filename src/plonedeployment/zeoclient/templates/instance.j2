#!{{ options.python }}

import plone.recipe.zope2instance.ctl
import sys

if __name__ == "__main__":
    sys.exit(
        plone.recipe.zope2instance.ctl.main(
            [
                "-C",
                "{{ options.zope_conf_path }}",
                "-p",
                "{{ options.interpreter_path }}",
                "-w",
                "{{ options.wsgi_ini_path }}",
            ]
            + sys.argv[1:]
        )
    )
